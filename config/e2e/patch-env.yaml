apiVersion: apps/v1
kind: Deployment
metadata:
  name: etcd-operator
spec:
  template:
    spec:
      containers:
        - name: manager
          env:
            - name: GOFAIL_HTTP
              value: ":22381"

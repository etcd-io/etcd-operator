# permissions for end users to view etcdmirrors.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: etcd-operator
    app.kubernetes.io/managed-by: kustomize
  name: etcdmirror-viewer-role
rules:
- apiGroups:
  - operator.etcd.io
  resources:
  - etcdmirrors
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - operator.etcd.io
  resources:
  - etcdmirrors/status
  verbs:
  - get
